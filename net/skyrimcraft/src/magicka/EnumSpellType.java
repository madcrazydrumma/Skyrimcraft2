package net.skyrimcraft.src.magicka;

public enum EnumSpellType
{
	ALL(0),
	ALTERATION(1),
	DESTRUCTION(2),
	RESTORATION(3),
	SHOUT(4),
	POWERS(5);
	//ACTIVE_EFFECTS(6);
	
	private EnumSpellType(int id) {
	}
}